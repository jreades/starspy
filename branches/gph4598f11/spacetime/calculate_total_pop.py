import numpy as num

"""
calc_total_pop calculates the total underlying population "t"

calc w calculates the total 'population at risk' or the weights
"""
def calc_total_pop(points):
    calcu_total_pop = points.sum[4]
    total_pop = calcu_total_pop[4]

def w(points):
    calc_total_w = points.sum[1]
    total_w = calc_total_w[1]

print total_w 
print total_pop    

